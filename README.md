# Customer-Churn-Prediction-
The code assesses three classifiers—DecisionTree, RandomForest, and XGBoost—using GridSearchCV for hyperparameter tuning and 5-fold stratified CV. RandomForest achieves the top CV ROC-AUC (0.9200) and is selected as the final model for predictions and deployment
